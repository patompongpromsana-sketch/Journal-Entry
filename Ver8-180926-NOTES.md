# Ver8-180926 — Version Checkpoint

**บันทึกเมื่อ:** 18 กันยายน 2569 (2026-09-18)

## สถานะของเวอร์ชันนี้

จุดตรวจสอบนี้บันทึกโค้ดหลังจากที่ **ฟีเจอร์ "ลงทุน" (Investments) ถูกถอดออกทั้งหมด** จากแอป ตามคำขอของผู้ใช้ เนื่องจากปุ่ม "อัปเดตราคา" มีบั๊กที่แก้ไม่สำเร็จ และผู้ใช้ตัดสินใจรื้อฟีเจอร์นี้ออกทั้งหมดเพื่อไปออกแบบใหม่ในอนาคต

### สิ่งที่เปลี่ยนแปลงจากเวอร์ชันก่อนหน้า

- ลบแท็บ "ลงทุน" ออกจากเมนู (ทั้ง sidebar บนเดสก์ท็อปและแถบด้านล่างบนมือถือ) — `index.html`
- ลบโค้ดที่เกี่ยวกับการลงทุนทั้งหมดออกจาก `app.js`:
  - `state.holdings`, `state.investmentTxns`
  - ฟังก์ชันคำนวณ: `holdingValue`, `holdingCost`, `totalInvestmentValue`, `totalInvestmentCost`
  - การ์ดสรุปการลงทุนในหน้าภาพรวม (Dashboard)
  - โมดัลและฟอร์มทั้งหมด: เพิ่ม/แก้ไขสินทรัพย์, อัปเดตราคา, บันทึกซื้อ/ขาย
  - ตัวกรองประเภท "การลงทุน" ในหน้ารายการ
- **ข้อมูลการลงทุนเก่าที่มีอยู่จริงใน Supabase จะถูกลบทิ้งอัตโนมัติครั้งเดียว** เมื่อผู้ใช้ล็อกอินครั้งถัดไป ผ่านฟังก์ชัน `maybeCleanupInvestmentData()` ในลักษณะเดียวกับระบบ seed ข้อมูลเริ่มต้นที่มีอยู่แล้วในแอป (ตรวจสอบด้วย flag `state.meta.app.investmentDataCleaned` กันไม่ให้ลบซ้ำ) — ผู้ใช้เลือกตัวเลือกนี้เอง ("ลบทิ้งทั้งหมด เริ่มใหม่สะอาด")
- `style.css` **ยังไม่ได้ทำความสะอาด** — ยังมี CSS ของฟีเจอร์ลงทุนหลงเหลืออยู่ (`.holding-card`, `.invest-hero`, ตัวแปรสี `--invest`/`--invest-bg` ฯลฯ) แต่ไม่มีผลกระทบเพราะไม่มี element ไหนใช้ class เหล่านี้แล้ว
- `supabase_setup.sql` ไม่มีการเปลี่ยนแปลง (โครงสร้างตาราง `user_data` ยังเหมือนเดิม)

### ตรวจสอบแล้ว
- `node --check app.js` ผ่าน (ไม่มี syntax error)
- grep หาคำที่เกี่ยวกับการลงทุน (`holding`, `investTx`, `investment` ฯลฯ) ไม่พบส่วนที่หลงเหลือ ยกเว้นโค้ดล้างข้อมูลอัตโนมัติที่ตั้งใจเขียนไว้

### แผนต่อไป (ยังไม่ได้ทำ)
- ออกแบบโครงสร้างฟีเจอร์ "ลงทุน" ใหม่ก่อนเริ่มพัฒนาใหม่
- (ทางเลือก) ทำความสะอาด CSS ของการลงทุนที่เหลืออยู่ใน `style.css`
- ผู้ใช้อาจต้องการลบบัญชีทดสอบ Supabase ที่สร้างไว้ระหว่างการดีบัก (`patompongpromsana+claudetest@gmail.com`) ด้วยตนเองจาก Supabase > Authentication > Users

## ไฟล์ในเวอร์ชันนี้
- `versions/Ver8-180926-app.js`
- `versions/Ver8-180926-index.html`
- `versions/Ver8-180926-style.css`
- `versions/Ver8-180926-supabase_setup.sql`
